name: Relabel Agentic Workflow Issues

on:
  issues:
    types: [labeled]

# Top-level: no permissions; granted per-job only
permissions: {}

concurrency:
  group: relabel-agentic-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  relabel:
    if: github.event.label.name == 'agentic-workflows'
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;

            // Add the canonical labels
            await github.rest.issues.addLabels({ owner, repo, issue_number, labels: ['bot/skip-grooming', 'bot/agentic-workflows'] });

            // Remove the source label (ignore 404 if already removed)
            try {
              await github.rest.issues.removeLabel({ owner, repo, issue_number, name: 'agentic-workflows' });
            } catch (error) {
              if (error.status !== 404) throw error;
            }
